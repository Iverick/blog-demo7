<%= form_with(model: [@post, @post.comments.build]) do |form| %>
  <div class="form-control mt-4">
    <%= form.rich_text_area :body %>
    <%= form.submit "Reply", class: "btn btn-sm btn-primary mt-1" %>
  </div>
<% end %>