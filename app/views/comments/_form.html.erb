<%= form_with(model: [post, comment]) do |form| %>
  <div class="form-control mt-4">
    <%= form.rich_text_area :body %>
    <%= form.submit submit_label, class: "btn btn-sm btn-primary mt-1" %>
  </div>
<% end %>